<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (Material3) -->
    <style name="Base.Theme.ExpiryTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Default/light customizations go here -->
        <!-- Example: <item name="colorPrimary">@color/action_green</item> -->
    </style>

    <!-- App theme -->
    <style name="Theme.ExpiryTracker" parent="Base.Theme.ExpiryTracker" />

    <!-- Fullscreen variant -->
    <style name="Theme.ExpiryTracker.FullScreen" parent="Theme.ExpiryTracker">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>